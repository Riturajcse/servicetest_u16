language: java

jdk:
  - openjdk8

services:
  - postgres 

build:
  ci:
    - node --version    
    - psql --version
        
  #pre_ci_boot:
    #image_name: drydock/u16all
    #image_tag: master
    #pull: true    
